"""Stub: experiment runner helpers — originally from exp004_5l and exp004_5k.

Provide the real implementations or install the packages to use.
"""


def run_phase_a_training(seed, condition):
    """Originally from run_004_5n1 in exp004_5l_instance_outcome_memory."""
    raise NotImplementedError(
        "run_phase_a_training is from exp004_5l_instance_outcome_memory. "
        "Provide the real implementation before running."
    )


def run_cluster_baselines(train_objects, student, train_env, base_learner,
                          coverage, seed):
    """Originally from run_004_5l in exp004_5l_instance_outcome_memory."""
    raise NotImplementedError(
        "run_cluster_baselines is from exp004_5l_instance_outcome_memory. "
        "Provide the real implementation before running."
    )


def collect_sparse_probe_outcomes(train_objects, student, coverage, seed):
    """Originally from sparse_outcome_collector in exp004_5k."""
    raise NotImplementedError(
        "collect_sparse_probe_outcomes is from exp004_5k_episodic_sparse_outcome. "
        "Provide the real implementation before running."
    )
